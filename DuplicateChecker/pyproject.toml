[project]
name = "duplicate-checker"
version = "2.0.0"
description = "Shop Duplicate Detection System"
requires-python = ">=3.8"
dependencies = [
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "scipy>=1.10.0",
    "openpyxl>=3.1.0",
]
